<!-- START LEFT SIDEBAR NAV-->
      <aside id="left-sidebar-nav">
        <ul id="slide-out" class="side-nav leftside-navigation">
            <li class="user-details cyan darken-2">
                <div class="row">
                    <div class="col col s4 m4 l4">
                        <img src="{% if app.user != null and app.user.imgprofil != null %}{{ asset(app.user.imgprofil.getwebpath) }}{% else %}{{ asset('template/images/projets/afrique.png') }}{% endif %}" alt="" class="circle responsive-img valign profile-image">
                    </div>
                    <div class="col col s8 m8 l8">
                        <ul id="profile-dropdown" class="dropdown-content">
                            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
							<li><a href="{{ path('users_user_user_accueil', {'id': app.user.id }) }}"> {{ app.user.name(20) }}</a>
                            </li>
							{% else %}
							<li><a href="{{ path('login') }}"> Connexion</a>
                            </li>
							{% endif %}
							{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                            <li><a href="{{ path('logout') }}"> Déconnexion</a>
                            </li>
							{% else %}
							<li><a href="{{ path('users_user_inscription_user') }}"> S'inscrire</a>
                            </li>
							{% endif %}
                            
                        </ul>
						{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <a class="btn-flat dropdown-button waves-effect waves-light white-text profile-btn" href="{{ path('users_user_user_accueil', {'id': app.user.id }) }}" data-activates="profile-dropdown"> {{ app.user.name(20) }} <i class="mdi-navigation-arrow-drop-down right"></i></a>
                        <p class="user-roal">Abonné</p>
						{% else %}
						<a class="btn-flat dropdown-button waves-effect waves-light white-text profile-btn" href="#" data-activates="profile-dropdown"> Anonyme <i class="mdi-navigation-arrow-drop-down right"></i></a>
                        <p class="user-roal">Non connecté</p>
						{% endif %}
                    </div>
                </div>
            </li>
            
            <li class="no-padding">
                <ul class="collapsible collapsible-accordion">
					
					<li class="bold"><a href="{{ path('users_user_acces_plateforme') }}" class="waves-effect waves-cyan"><i class="mdi-maps-store-mall-directory"></i> Accueil</a>
					</li>
					{% for scat in sous_categorie %}
					<li class="bold"><a href="{{ path('produit_produit_liste_produit_souscategorie', {'id': scat.id }) }}" class="waves-effect waves-cyan">{{ scat.faicon|raw }} {{ scat.nom }}</a>
					</li>
					{% endfor %}
                    <li class="bold">
					<a class="collapsible-header waves-effect waves-cyan"><i class="mdi-image-assistant-photo"></i> Langue</a>
                        <div class="collapsible-body">
                            <ul>
                                <li><a href="#!" class="-text" style="color: #66afe9;"> Français </a>
                                </li>                                        
                                <li><a href="#!" class="-text" style="color: #66afe9;"> Anglais </a>
                                </li>
                            </ul>
                        </div>
                    </li>

                </ul>
            </li>

        </ul>
        <a href="#" data-activates="slide-out" class="sidebar-collapse btn-floating btn-medium waves-effect waves-light hide-on-large-only cyan"><i class="mdi-navigation-menu"></i></a>
      </aside>
      <!-- END LEFT SIDEBAR NAV-->